<Page
    x:Class="ZlecajGoWpfApp.Views.UserAccountMenuNavigationPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:viewModel="clr-namespace:ZlecajGoWpfApp.ViewModels"
    d:DataContext="{d:DesignInstance Type=viewModel:UserAccountMenuViewModel,
                                     IsDesignTimeCreatable=True}"
    mc:Ignorable="d">

    <Page.Resources>
        <Style
            x:Key="ButtonIcon"
            TargetType="materialDesign:PackIcon">
            <Setter Property="Width" Value="72"/>
            <Setter Property="Height" Value="72"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style
            x:Key="ButtonText"
            TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="TextAlignment" Value="Center"/>
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="200"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="150"/>
        </Grid.ColumnDefinitions>

        <Button
            Grid.Row="0"
            Grid.Column="0"
            VerticalContentAlignment="Stretch"
            Style="{StaticResource HoverTriggerButton}"
            Command="{Binding NavigateToUserOffersCommand}">
            <Button.Content>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <materialDesign:PackIcon
                        Grid.Row="0"
                        Style="{StaticResource ButtonIcon}"
                        Kind="TextAccount"/>
                    <TextBlock
                        Grid.Row="1"
                        Style="{StaticResource ButtonText}"
                        Text="Moje oferty"/>
                </Grid>
            </Button.Content>
        </Button>
        <Button
            Grid.Row="0"
            Grid.Column="1"
            VerticalContentAlignment="Stretch"
            Style="{StaticResource HoverTriggerButton}"
            Command="{Binding NavigateToUserDetailsCommand}">
            <Button.Content>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <materialDesign:PackIcon
                        Grid.Row="0"
                        Style="{StaticResource ButtonIcon}"
                        Kind="BadgeAccount"/>
                    <TextBlock
                        Grid.Row="1"
                        Style="{StaticResource ButtonText}"
                        Text="Dane użytkownika"/>
                </Grid>
            </Button.Content>
        </Button>
        <Button
            Grid.Row="1"
            Grid.Column="0"
            Grid.ColumnSpan="2"
            Command="{Binding CloseWindowCommand}"
            Content="Wróć"/>
    </Grid>
</Page>
